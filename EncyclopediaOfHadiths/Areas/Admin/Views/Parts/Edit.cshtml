@model EncyclopediaOfHadiths.Models.Part

@{
    ViewData["Title"] = @Localizer["Edit"];
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
<div class="title-right-rtl">
<h1>@Localizer["Edit"]</h1>

<h4>@Localizer["Part"]</h4>
</div>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="PartId" />
            <div class="form-group">
                <label asp-for="PartTitle" class="control-label"></label>
                <input asp-for="PartTitle" class="form-control" />
                <span asp-validation-for="PartTitle" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="@Localizer["Save"]" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div class="title-right-rtl">
    <a asp-action="Index">@Localizer["Back to List"]</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
